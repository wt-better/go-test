package go_test

import (
	"fmt"
	"testing"
)

type Manager struct {
	appName string
}

func Test1(t *testing.T) {
	//var mnr = map[string]Manager{}
	//s := mnr[""]
	//println("s:" + s.appName)
	print(fmt.Sprintf("feed %v is not ck transport", "123"))
}
